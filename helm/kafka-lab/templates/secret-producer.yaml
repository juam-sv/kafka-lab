{{- if not .Values.externalSecrets.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kafka-lab.producerSecretName" . }}
  labels:
    {{- include "kafka-lab.labels" . | nindent 4 }}
type: Opaque
stringData:
  KAFKA_BROKER: {{ .Values.kafka.brokers | quote }}
{{- end }}
